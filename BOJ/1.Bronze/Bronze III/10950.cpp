#include <bits/stdc++.h>

int main(int argc, char* argv[])
{
    int T, a, b;
    std::cin >> T;
    while(T--)
    {
        std::cin >> a >> b;
        std::cout << a + b << '\n';
    }
    return 0;
}