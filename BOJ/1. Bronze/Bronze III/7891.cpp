#include <bits/stdc++.h>

int main(int argc, char* argv[])
{
    int T;
    std::cin >> T;
    while(T--)
    {
        long long a, b, sum = 0;
        std::cin >> a >> b;
        sum = a + b;
        std::cout << sum << '\n';
    }
    return 0;
}